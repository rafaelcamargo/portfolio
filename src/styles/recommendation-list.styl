@require '_mixins'

.r-recommendation-list
  display grid
  grid-template-columns 1fr 1fr
  grid-gap 100px 20px
  margin-bottom -100px

+breakpoint('xs')
  .r-recommendation-list
    grid-template-columns 1fr
    margin-bottom 0
